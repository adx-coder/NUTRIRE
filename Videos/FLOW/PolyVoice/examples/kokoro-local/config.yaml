runtime:
  host: 127.0.0.1
  port: 8092
  log_level: info

stt:
  provider: mock
  model: mock

llm:
  provider: mock
  model: mock

tts:
  provider: tts-sdk
  model: kokoro
  params:
    provider_name: kokoro
    voice: af_heart
    speed: 1.0
    sample_rate: 24000
    sdk_config:
      providers:
        - provider: local_model
          model_loader: kokoro
          name: kokoro
          language: en
          voice: af_heart
          device: cpu
          repo_id: hexgrad/Kokoro-82M
      audio_pipeline:
        target_sample_rate: 24000
